#include <stdio.h>

int main(void) {
    int estoque = 12;
    int reposicao = 8;
    int total = estoque + reposicao;

    printf("Estoque inicial: %d\n", estoque);
    printf("Reposicao: %d\n", reposicao);
    printf("Estoque final: %d\n", total);

    return 0;
}
