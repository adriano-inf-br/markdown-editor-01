#include <stdio.h>

int main(void) {
    int pendentes = 12;
    int separacao = 5;
    int enviados = 21;

    printf("Pendentes: %d\n", pendentes);
    printf("Em separacao: %d\n", separacao);
    printf("Enviados: %d\n", enviados);

    return 0;
}
